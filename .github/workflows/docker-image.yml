name: Deploy to NAS

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install Cloudflared
        run: |
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          
          # Configure SSH to tunnel through Cloudflare
          # NOTE: Ensure 'ssh-moneda' matches your Cloudflare Public Hostname
          echo "Host nas    
            HostName "${{ secrets.NAS_HOST }}" 
            User "${{ secrets.NAS_USER }}"
            IdentityFile ~/.ssh/id_ed25519
            StrictHostKeyChecking no
            ProxyCommand cloudflared access ssh --hostname %h" >> ~/.ssh/config

      - name: Deploy Files
        run: |
          # 1. Connect to NAS
          # 2. Go to your project folder
          # 3. Git fetch and reset to latest main
          # 4. Restart Docker containers (except CF tunnel)
          ssh nas "cd /volume1/docker/moneda-bio && git fetch origin main && git reset --hard origin/main && docker-compose restart backend frontend"